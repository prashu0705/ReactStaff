# ReactStaff Backend

Deterministic team composition engine backend.

## Setup & Run

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the server:
   ```bash
   uvicorn main:app --reload
   ```

3. Open API docs at HTTP: [http://localhost:8000/docs](http://localhost:8000/docs)
