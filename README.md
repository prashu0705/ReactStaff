# ReactStaff — Frontend

Quick start

1. Install dependencies

```powershell
cd frontend && npm install
```

2. Run the dev server

```powershell
npm run dev  # runs at localhost:3000
```

Note

- The backend must be running at http://localhost:8000 before using the app.
